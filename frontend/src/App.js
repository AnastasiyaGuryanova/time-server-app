import { TimeDisplay } from "./components/TimeDisplay";

export const App = () => {
  return (
    <div style={{ minHeight: "100vh", backgroundColor: "#f5f5f5" }}>
      <TimeDisplay />
    </div>
  );
};
